name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Install tools
        run: brew install swiftlint swift-format periphery
      - name: Build and test
        run: make build && make test
      - name: Format lint
        continue-on-error: true
        run: make lint-format
      - name: Guardrails
        run: make check-guardrails
      - name: Dead-code scan
        run: make check-deadcode
      - name: Lint
        run: make lint
